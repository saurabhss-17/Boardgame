apiVersion: v1
kind: Service
metadata:
  name: boardgame-app-service
  namespace: capstone
  annotations: 
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: boardgame-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080